import React from 'react';
import './Loading.css';

const Loading = () => {
    return (
        <div className="loading-indicator">
            <p>Carregando...</p>
        </div>
    );
};

export default Loading;
